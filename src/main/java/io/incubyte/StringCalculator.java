package io.incubyte;

public class StringCalculator {

    int add(String numbers) {

        if(numbers == null || numbers.length() == 0) {
            return 0;
        }

        return 1;
    }
}
